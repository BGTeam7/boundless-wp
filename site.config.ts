type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Boundless Gamers",
  site_description: "Spreading happiness one game at a time.",
  site_domain: "https://next-wp.com",
};
